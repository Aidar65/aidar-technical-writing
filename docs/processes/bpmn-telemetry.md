# Процесс обработки аварийного момента (BPMN 2.0)`n`n```mermaid`ngraph TD`n    A([Старт: Превышение момента]) --> B[Считывание показаний датчика тока]`n    B --> C{Момент > 250 Нм?}`n    C -- Нет --> D[Запись в лог штатной телеметрии]`n    C -- Да --> E[Формирование аварийного кадра Modbus]`n    E --> F[Команда Cut-off на ПЛК]`n    F --> G([Конец: Фиксация аварии])`n```
